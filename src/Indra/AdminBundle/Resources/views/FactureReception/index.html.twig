{% extends '::base.html.twig' %}

{% block body -%}
    <h1>FactureReception list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Statut</th>
                <th>Datearrive</th>
                <th>Datedepart</th>
                <th>Paye</th>
                <th>Receptionniste</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('facturereception_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.statut }}</td>
                <td>{{ entity.dateArrive }}</td>
                <td>{{ entity.dateDepart }}</td>
                <td>{{ entity.paye }}</td>
                <td>{{ entity.receptionniste }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('facturereception_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('facturereception_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('facturereception_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
